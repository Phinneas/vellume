{
  "name": "vellume-api",
  "version": "1.0.0",
  "description": "Vellume API - Cloudflare Workers backend",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute vellume-db --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute vellume-db --local --file=schema.sql"
  },
  "dependencies": {
    "stripe": "^14.14.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
